[package]
name = "launchboard-desktop"
version = "0.2.0"
description = "Launchboard desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2.3.5"
